#include "SFML/System.hpp"
#pragma once

namespace Directions {
    const sf::Vector2f Left(-1, 0);
    const sf::Vector2f Right(1, 0);
    const sf::Vector2f Up(0, -1);
    const sf::Vector2f Down(0, 1);
    const sf::Vector2f NullDirection(0, 0);
}